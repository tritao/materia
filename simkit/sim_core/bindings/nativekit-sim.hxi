// Generated by Haxeon from sim_core/bindings/nativekit_sim_import.h for portable-abi64. Do not edit.
interface NativeKitSim @target("portable-abi64") @library("nativekit_sim_core") @depends("NativeKitScene") {
	const NKSIM_ERROR_BACKEND = -8;
	const NKSIM_ERROR_INVALID_ARGUMENT = -1;
	const NKSIM_ERROR_INVALID_HANDLE = -2;
	const NKSIM_ERROR_INVALID_STATE = -3;
	const NKSIM_ERROR_OUT_OF_MEMORY = -4;
	const NKSIM_ERROR_SCENE = -9;
	const NKSIM_ERROR_STALE_ID = -6;
	const NKSIM_ERROR_UNSUPPORTED = -5;
	const NKSIM_ERROR_WRONG_THREAD = -7;
	const NKSIM_JOINT_FIXED = 1;
	const NKSIM_JOINT_PRISMATIC = 3;
	const NKSIM_JOINT_REVOLUTE = 2;
	const NKSIM_JOINT_TARGET_EFFORT = 3;
	const NKSIM_JOINT_TARGET_POSITION = 1;
	const NKSIM_JOINT_TARGET_VELOCITY = 2;
	const NKSIM_MOTION_DYNAMIC = 2;
	const NKSIM_MOTION_KINEMATIC = 1;
	const NKSIM_MOTION_STATIC = 0;
	const NKSIM_OK = 0;
	const NKSIM_SHAPE_BOX = 1;
	const NKSIM_SHAPE_CAPSULE = 3;
	const NKSIM_SHAPE_PLANE = 4;
	const NKSIM_SHAPE_SPHERE = 2;
	const NKSIM_SNAPSHOT_PAGE_CAPACITY = 64;
	extern fn nksim_body_create(world: nksim_world, desc: ptr<const<nksim_body_desc>>, out_body: ptr<nksim_body> @out) -> nksim_result;
	extern fn nksim_body_destroy(world: nksim_world, body: nksim_body) -> void;
	extern fn nksim_body_get_state(world: nksim_world, body: nksim_body, out_state: ptr<nksim_body_state> @inout) -> nksim_result;
	extern fn nksim_body_reset(world: nksim_world, body: nksim_body) -> nksim_result;
	/**
	 * Replaces a body's pose and velocities on the world owner thread. For a
	 * kinematic body this is a discontinuity: the next tick keeps the supplied
	 * velocities instead of inferring them from the scene-node pose change.
	 */
	extern fn nksim_body_set_state(world: nksim_world, body: nksim_body, state: ptr<const<nksim_body_state>>) -> nksim_result;
	extern fn nksim_joint_create(world: nksim_world, desc: ptr<const<nksim_joint_desc>>, out_joint: ptr<nksim_joint> @out) -> nksim_result;
	extern fn nksim_joint_destroy(world: nksim_world, joint: nksim_joint) -> void;
	extern fn nksim_joint_get_state(world: nksim_world, joint: nksim_joint, out_state: ptr<nksim_joint_state> @inout) -> nksim_result;
	extern fn nksim_shape_create(world: nksim_world, desc: ptr<const<nksim_shape_desc>>, out_shape: ptr<nksim_shape> @out) -> nksim_result;
	extern fn nksim_shape_create_box(world: nksim_world, half_extents: ptr<const<f64>>, out_shape: ptr<nksim_shape> @out) -> nksim_result;
	extern fn nksim_shape_create_capsule(world: nksim_world, radius: f64, height: f64, out_shape: ptr<nksim_shape> @out) -> nksim_result;
	extern fn nksim_shape_create_plane(world: nksim_world, normal: ptr<const<f64>>, offset: f64, out_shape: ptr<nksim_shape> @out) -> nksim_result;
	extern fn nksim_shape_create_sphere(world: nksim_world, radius: f64, out_shape: ptr<nksim_shape> @out) -> nksim_result;
	extern fn nksim_shape_destroy(world: nksim_world, shape: nksim_shape) -> void;
	extern fn nksim_snapshot_destroy(snapshot: nksim_snapshot) -> void;
	extern fn nksim_snapshot_get_body(snapshot: nksim_snapshot, index: u64, out_state: ptr<nksim_body_state> @inout) -> nksim_result;
	extern fn nksim_snapshot_get_body_count(snapshot: nksim_snapshot, out_count: ptr<u64> @out) -> nksim_result;
	extern fn nksim_snapshot_get_body_page(snapshot: nksim_snapshot, start_index: u64, out_page: ptr<nksim_snapshot_body_page> @inout) -> nksim_result;
	extern fn nksim_snapshot_get_clock(snapshot: nksim_snapshot, out_clock: ptr<nksim_clock> @inout) -> nksim_result;
	extern fn nksim_snapshot_get_joint(snapshot: nksim_snapshot, index: u64, out_state: ptr<nksim_joint_state> @inout) -> nksim_result;
	extern fn nksim_snapshot_get_joint_count(snapshot: nksim_snapshot, out_count: ptr<u64> @out) -> nksim_result;
	extern fn nksim_snapshot_get_joint_page(snapshot: nksim_snapshot, start_index: u64, out_page: ptr<nksim_snapshot_joint_page> @inout) -> nksim_result;
	extern fn nksim_world_apply_forces(world: nksim_world, forces: ptr<const<nksim_body_force>> @in_array("count"), count: u32) -> nksim_result;
	/**
	 * Stages body and joint additions before the first world step.
	 */
	extern fn nksim_world_begin_topology_update(world: nksim_world) -> nksim_result;
	extern fn nksim_world_create(desc: ptr<const<nksim_world_desc>>, out_world: ptr<nksim_world> @out @owned) -> nksim_result;
	extern fn nksim_world_destroy(world: nksim_world) -> void;
	/**
	 * Commits a staged topology update with one backend rebuild.
	 */
	extern fn nksim_world_end_topology_update(world: nksim_world) -> nksim_result;
	extern fn nksim_world_get_clock(world: nksim_world, out_clock: ptr<nksim_clock> @inout) -> nksim_result;
	/**
	 * Restores body state and the fixed-step clock to their initial values.
	 */
	extern fn nksim_world_reset(world: nksim_world) -> nksim_result;
	extern fn nksim_world_set_joint_targets(world: nksim_world, targets: ptr<const<nksim_joint_target>> @in_array("count"), count: u32) -> nksim_result;
	extern fn nksim_world_snapshot(world: nksim_world, out_snapshot: ptr<nksim_snapshot> @out @owned) -> nksim_result;
	extern fn nksim_world_step(world: nksim_world, out_result: ptr<nksim_step_result> @inout) -> nksim_result;
	struct nksim_body_desc @layout(160, 8) {
		struct_size: u32 @offset(0) @struct_size;
		node: nkscene_node_id @offset(8);
		motion_type: u32 @offset(16);
		mass: f64 @offset(24);
		shape: nksim_shape @offset(32);
		collision_layer: u32 @offset(36);
		collision_mask: u32 @offset(40);
		has_inertial_properties: u32 @offset(44);
		center_of_mass: array<f64, 3> @offset(48);
		/**
		 * < Row-major symmetric tensor in kg m².
		 */
		inertia_tensor: array<f64, 9> @offset(72);
		reserved: array<u64, 2> @offset(144);
	}
	struct nksim_body_force @layout(56, 8) {
		struct_size: u32 @offset(0) @struct_size;
		body: nksim_body @offset(4);
		force: array<f64, 3> @offset(8);
		torque: array<f64, 3> @offset(32);
	}
	struct nksim_body_state @layout(144, 8) {
		struct_size: u32 @offset(0) @struct_size;
		body: nksim_body @offset(4);
		node: nkscene_node_id @offset(8);
		position: array<f64, 3> @offset(16);
		rotation: array<f64, 4> @offset(40);
		linear_velocity: array<f64, 3> @offset(72);
		angular_velocity: array<f64, 3> @offset(96);
		sleeping: u32 @offset(120);
		reserved0: u32 @offset(124);
		reserved: array<u64, 2> @offset(128);
	}
	struct nksim_clock @layout(32, 8) {
		struct_size: u32 @offset(0) @struct_size;
		time: f64 @offset(8);
		fixed_timestep: f64 @offset(16);
		step_index: u64 @offset(24);
	}
	struct nksim_joint_desc @layout(208, 8) {
		struct_size: u32 @offset(0) @struct_size;
		type: u32 @offset(4);
		body_a: nksim_body @offset(8);
		body_b: nksim_body @offset(12);
		anchor_a: array<f64, 3> @offset(16);
		anchor_b: array<f64, 3> @offset(40);
		/**
		 * < Unit vector in body_a's own frame.
		 */
		axis_a: array<f64, 3> @offset(64);
		lower_limit: f64 @offset(88);
		upper_limit: f64 @offset(96);
		max_force: f64 @offset(104);
		/**
		 * Joint-frame orientation relative to body_a/body_b (x, y, z, w),
		 * appended after the original fields so an old struct prefix remains
		 * valid. A struct_size smaller than sizeof(nksim_joint_desc) means the
		 * caller predates these fields; backends must treat them as identity
		 * rather than reading past the caller's actual struct.
		 */
		rotation_a: array<f64, 4> @offset(112);
		rotation_b: array<f64, 4> @offset(144);
		reserved: array<u64, 4> @offset(176);
	}
	struct nksim_joint_state @layout(56, 8) {
		struct_size: u32 @offset(0) @struct_size;
		joint: nksim_joint @offset(4);
		position: f64 @offset(8);
		velocity: f64 @offset(16);
		effort: f64 @offset(24);
		reserved0: u32 @offset(32);
		reserved: array<u64, 2> @offset(40);
	}
	struct nksim_joint_target @layout(32, 8) {
		struct_size: u32 @offset(0) @struct_size;
		joint: nksim_joint @offset(4);
		mode: u32 @offset(8);
		target: f64 @offset(16);
		max_force: f64 @offset(24);
	}
	struct nksim_shape_desc @layout(56, 8) {
		struct_size: u32 @offset(0) @struct_size;
		type: u32 @offset(4);
		parameters: array<f64, 4> @offset(8);
		reserved: array<u64, 2> @offset(40);
	}
	struct nksim_snapshot_body_page @layout(9240, 8) {
		struct_size: u32 @offset(0) @struct_size;
		start_index: u64 @offset(8);
		count: u32 @offset(16);
		bodies: array<nksim_body_state, 64> @offset(24);
	}
	struct nksim_snapshot_joint_page @layout(3608, 8) {
		struct_size: u32 @offset(0) @struct_size;
		start_index: u64 @offset(8);
		count: u32 @offset(16);
		joints: array<nksim_joint_state, 64> @offset(24);
	}
	struct nksim_step_result @layout(64, 8) {
		struct_size: u32 @offset(0) @struct_size;
		step_index: u64 @offset(8);
		simulation_time: f64 @offset(16);
		physics_substeps: u32 @offset(24);
		scene_changes: nkscene_change_set @offset(28);
		reserved: array<u64, 4> @offset(32);
	}
	struct nksim_world_desc @layout(80, 8) {
		struct_size: u32 @offset(0) @struct_size;
		scene: nkscene_scene @offset(4);
		fixed_timestep: f64 @offset(8);
		physics_substeps: u32 @offset(16);
		gravity: array<f64, 3> @offset(24);
		reserved: array<u64, 4> @offset(48);
	}
	handle nksim_body : u32;
	handle nksim_joint : u32;
	type nksim_result = i32;
	handle nksim_shape : u32;
	handle nksim_snapshot : u32 @destroy("nksim_snapshot_destroy");
	handle nksim_world : u32 @destroy("nksim_world_destroy");
}
